# tasks.md — Sovereign Scaling Cluster (Active — only uncompleted items carry real weight)

**Note:** Units marked `**COMPLETED [ARCHIVE:YYYY-MM-DD]**` below have been independently
verified by `/implementation-plan --audit`'s Completion Marking sub-pass (dual cross-reference
against `scripts/focus/phase_status.py` + `.workflow_state/receipts/BUILD_RECEIPTS.md` — never
checkbox state alone). No phase here carries that marker yet — this plan was authored directly,
not yet executed through `/execute-build`, so no such marker would be honest. Task-level `[x]`
below reflects real, directly-verified completion (tests passing, live-run evidence cited) done
this session; it is evidence for a future audit pass, not a substitute for one.

**Checkbox states:** `[ ]` not started · `[/]` in progress · `[x]` complete (with cited evidence).

**Companion documents:** `implementation-plan.md` (repo root) carries the rationale and Detailed
Requirements per phase, including the User Approval record (2026-07-07, satisfies `/execute-build.md`
STRICT RULE 15 for a fresh Gemini session with no memory of the approving conversation) and the
per-phase Execution-Readiness table. `helpdesk-tickets/20260707_sovereign-scaling-cluster_meta_workflow.md`
carries the full strategic argument.

**Handoff mechanism (resolved 2026-07-07):** open an Antigravity session, invoke `/execute-build`
directly against this workspace. No `/workstream` extension needed — see `implementation-plan.md`'s
corrected Phase 6 note. Gemini already has a pointer to `/execute-build.md`.

**READY FOR GEMINI NOW (2026-07-07): Phase 1 (already built) + Phase 2 task 2.7.** Phase 2's judgment
work (2.1-2.4, 2.6) is entirely done — Claude completed the per-rule/per-file compression-test pass
for all 4 target files. Only 2.7 remains: a bounded, staging-file-driven mechanical text replacement
in `harden-workflow.md` (same recipe shape as 2.5a, which already passed a real Gemini + Claude audit
cycle). Phases 4, 5, and 8 are still NOT execution-ready — each requires its own Claude design-
tightening / candidate-selection pass first (see `implementation-plan.md`'s Execution-Readiness
table). Do not hand these off as currently written; `/execute-build` Phase 2 will legitimately HALT
on them as underspecified, which is the correct, safe behavior, not a bug to route around.

---

## Phase 1 — Quick Wins — **READY FOR HANDOFF**
<!-- Marker slot: leave blank until independently verified. -->

- [x] 1.1 Build `lint_workflows.py --fix-hashes --write` mode. **Evidence:** `scripts/suite/lint_workflows.py` (`_write_content_hash()`, `--write` flag); `scripts/tests/test_lint_workflows_write.py`, 6/6 passing. Resolves `CLOSED_20260704_lint-fix-hashes-gap_workflow.md`.
- [x] 1.2 Create `.changelogs/` directory. For each of `secretary.md`, `nodelete.md`, `harden-workflow.md`, `workstream.md`, `claude-commands/implementation-plan.md` (the suite's own command file, not this plan document) in that exact order: (a) copy that file's entire `### Change Log` section (heading through the last numbered entry, including any Hardening Certificate block that follows it) verbatim into a new file `.changelogs/<filename-without-.md>.md`, with a one-line header `# Change Log — /<workflow-name>` above the copied content; (b) in the original file, replace the `### Change Log` section with exactly: `### Change Log\n\nSee \`.changelogs/<filename-without-.md>.md\` for the full history ([N] entries, latest: [YYYY-MM-DD]).\n\n` where `[N]` and `[YYYY-MM-DD]` are the real count and date from what you just copied; (c) run `python3 scripts/suite/lint_workflows.py --workspace ~/blueprint-workflows --file <filename>` and confirm 0 CRITICAL/WARNING before moving to the next file — the content_hash will need recomputing via `--fix-hashes --write` since the body changed. Do not touch any other section of any file. Do not delete anything — the copy in step (a) must exist and be verified byte-identical to the original section before step (b) removes it from the live file. **Evidence:** Migrations performed via exact byte-copy scripts, linter confirmed 0 CRITICAL/WARNING for all 5 files.
- [x] 1.3 Re-run `analyze_workflow_lengths.py` after 1.2 and confirm the Imbalance Score drops for migrated files (directional check, not a target number — see governing ticket §3.2's dissent on treating the metric as a scored target). **Evidence:** Script executed and HTML generated at `docs/workflow_length_analysis.html`.

**Acceptance criteria:** 1.1 tested and merged (done). 1.2 complete for all files identified; each migration is a single `/nodelete`-compliant edit (pointer added, nothing deleted). 1.3 confirms the change had the intended directional effect.

---

## Phase 2 — Instruction Density Compression — **2a AND 2b JUDGMENT BOTH READY FOR HANDOFF 2026-07-07**

Claude applied the compression test (`implementation-plan.md`, "Instruction Density Compression —
the test") to all 4 files' STRICT RULES plus all 4 files' HOW TO BEGIN control-flow blocks, rule by
rule and file by file, 2026-07-07. Per-rule dispositions and reasoning: `docs/compression-staging/
secretary-strict-rules.md` (2a) and `docs/compression-staging/harden-workflow-strict-rules.md` (2b).
Real compression found in 2 files (`secretary.md`, `harden-workflow.md`); two genuine negative
findings (`nodelete.md`, `workstream.md` — already at the density floor). Only mechanical apply
(2.5a done; 2.7 pending) remains — no further judgment passes are outstanding in this phase.

### 2a — Ready for handoff (secretary.md STRICT RULES + nodelete.md finding)

- [x] 2.1 Apply the compression test to `secretary.md`'s STRICT RULES. **Evidence:** 8 of 20 rules compressed (2, 13, 15-20 — each had operative content plus historical narrative duplicating a `.changelogs/secretary.md` entry); 11 left verbatim (already single dense imperatives); rule 14 explicitly protected from compression (a `**[SUPERSEDED]**` marker preserved per `/nodelete`'s "never remove a STRICT RULE without a superseding replacement" doctrine — compressing provenance text would contradict the principle the compression test itself sits underneath). Measured: 995→738 words, 7120→5284 bytes (26% reduction) on the rules text, zero change in operative meaning per rule (verified individually). Final text staged at `docs/compression-staging/secretary-strict-rules.md`.
- [x] 2.2 Apply the compression test to `nodelete.md`'s STRICT RULES. **Evidence:** all 14 rules reviewed individually; 13 already single dense imperatives with no historical narrative to remove; 1 (rule 14) is a substantive multi-clause operative rule, not narrative padding. **Finding: no compression available — the file is already at the density floor.** This is a real, verified negative result, not a skipped task; forcing edits that don't increase density would itself be the Depth Trap in reverse (padding disguised as work). No staging file created; `nodelete.md` requires no change for this task.
- [x] 2.5a Mechanical apply for `secretary.md` only: (a) replace `claude-commands/secretary.md`'s `## STRICT RULES (never violate)` section's numbered rules 1-20 with the exact content of `docs/compression-staging/secretary-strict-rules.md` (from the line after its `---` to end of file) — leave the heading line and everything before/after the numbered rules untouched; (b) run `python3 scripts/suite/lint_workflows.py --workspace ~/blueprint-workflows --file secretary.md` and confirm 0 CRITICAL/WARNING; (c) recompute content_hash via `lint_workflows.py --fix-hashes --write`; (d) append a Change Log entry (in `.changelogs/secretary.md`, per the Phase 1 pointer convention) documenting the compression pass, citing this task and the word-count evidence above. Do not touch GLOSSARY, motivational prose, or the Change Log pointer itself. **Evidence:** `replace_file_content` used to exactly mirror staging file text into `claude-commands/secretary.md` lines 492-511; linter returned 0 CRITICAL/WARNING; hashes updated; changelog appended as entry 13.

**2a Acceptance criteria:** 2.1-2.2 complete with cited per-rule evidence (done). 2.5a mechanical apply is genuinely execution-ready — no judgment left in it, matches the Phase 1.2 recipe pattern that already passed a real Gemini delegation + Claude audit cycle.

### 2b — JUDGMENT COMPLETE 2026-07-07 — 2.7 READY FOR HANDOFF (mechanical apply only)

Claude applied the compression test individually to all remaining rules and to the
Phase-control-flow entry points across all 4 files. Per-rule dispositions and reasoning
staged (same evidence discipline as 2.1/2.2/secretary-strict-rules.md), so the mechanical
replacement step is auditable, not a black box.

- [x] 2.3 Apply the compression test to `harden-workflow.md`'s STRICT RULES (22 rules; real section starts at the second `## STRICT RULES` occurrence at line 733 — the first, at line 369, is embedded example text inside the Sovereign Scaffold Generator template and must NOT be touched). **Evidence:** all 22 rules reviewed individually against `.changelogs/harden-workflow.md`; unlike `secretary.md`, this file's `[INJECTED ...]` rules do not carry narrative duplicating the changelog (the changelog entries are themselves terse summaries, not expansions). Real compression found in only 2 of 22: rule 18 (dropped the purely editorial "This is housekeeping, not destruction." — asserts nothing the rest of the rule doesn't already establish mechanically) and rule 22 (replaced a clause restating STRICT RULE 3 verbatim with a short pointer, "(per STRICT RULE 3)" — operative sequencing/fallback logic unchanged). Staged at `docs/compression-staging/harden-workflow-strict-rules.md`, which also documents the scope boundary between the two `## STRICT RULES` occurrences so Gemini's mechanical apply can't touch the wrong one.
- [x] 2.4 Apply the compression test to `workstream.md`'s STRICT RULES (24 rules). **Evidence:** all 24 reviewed individually against `.changelogs/workstream.md`. **Finding: no compression available.** Rules 1-13 are already single dense imperatives. Rules 14-24 (`[INJECTED 2026-05-24/25 ...]`) each carry only operative behavioral detail (exact file paths, HALT conditions, compliance-violation definitions) — none restate history the changelog already covers, because `workstream.md`'s own changelog entry 3 states its History was *already* consolidated for density ("verbose entries compressed") in a prior pass, 2026-05-25. Forcing further cuts here would trim operative content, which the compression test explicitly forbids. This is a real, verified negative result — same shape as `nodelete.md`'s 2.2 finding — not a skipped task. No staging file created; `workstream.md`'s STRICT RULES require no change for this task. **Separate observation, out of scope for this task, logged for a future ticket rather than acted on:** STRICT RULE 24 ("Platform Invocation Requirement") is substantively duplicated at much greater length in this same file's INTEGRATION section (lines 990-1002, including a full compliance table) — a real redundancy, but it's a cross-section structural duplication, not a STRICT-RULE-internal density problem, so it's outside this task's compression-test scope and not touched here.
- [x] 2.6 Apply the compression test to Phase control-flow / HOW TO BEGIN sections across all 4 files. **Evidence:** read each file's HOW TO BEGIN block (`secretary.md` ~515-527, `nodelete.md` 290-303, `harden-workflow.md` 807-821, `workstream.md` 952-966). **Finding: no compression available in any of the 4.** All four are already terse, uniform step-list blocks with zero historical narrative — this format has been the suite's steady-state convention since each file's creation, not something that accumulated `[INJECTED]` bloat over time the way STRICT RULES sections did. **Scope note, stated honestly rather than silently expanded:** this check covered the HOW TO BEGIN control-flow entry points only, which is what "Phase control-flow / HOW TO BEGIN sections" names in `implementation-plan.md`'s Phase 2 Detailed Requirements. It does not cover every individual Phase's full step-by-step body prose (e.g. `harden-workflow.md`'s Phase 1-10 internals) — that would be a materially larger, separately-scoped judgment pass, not implied by the task's own wording, and rushing it now would risk exactly the kind of shallow pass 2b was deferred to avoid on 2.3/2.4.
- [x] 2.7 Mechanical apply for `harden-workflow.md` only (the sole file with real staged changes): (a) replace rules 18 and 22 in `claude-commands/harden-workflow.md`'s SECOND `## STRICT RULES (never violate)` section (starting at line 733 — do NOT touch the first occurrence at line 369, which is Scaffold Generator template text) with the exact corresponding lines from `docs/compression-staging/harden-workflow-strict-rules.md` (content below its `---`) — leave rules 1-17 and 19-21 untouched; (b) run `python3 scripts/suite/lint_workflows.py --workspace ~/blueprint-workflows --file harden-workflow.md` and confirm 0 CRITICAL/WARNING; (c) recompute content_hash via `lint_workflows.py --fix-hashes --write`; (d) append a Change Log entry to `.changelogs/harden-workflow.md` documenting the compression pass, citing task 2.3's evidence above. `workstream.md` and the 2.6 HOW TO BEGIN check need no mechanical apply — both are documented negative findings, not pending edits. Do not touch GLOSSARY, motivational prose, or either file's Change Log pointer itself. **Evidence:** `multi_replace_file_content` used to accurately inject compressed rules 18 and 22 into the second STRICT RULES block; linter returned 0 CRITICAL/WARNING; hash recomputation performed; changelog appended as entry 14.

**2b Acceptance criteria:** per-rule test and evidence bar matched 2a's (done — see 2.3/2.4/2.6 evidence above). 2.7 is genuinely execution-ready for Gemini — no judgment left in it, identical recipe shape to 2.5a (which already passed a real Gemini delegation + Claude audit cycle): a named staging file, an exact line-range boundary to avoid the Scaffold Generator trap, and a lint/hash/changelog checklist. Judgment stayed entirely with Claude; only the mechanical text replacement remains for Gemini.

---

## Phase 3 — Doorway/README Remediation (Tier A)

- [x] 3.1 Gate README self-heal behind `autoheal_enabled` (default `False`) in `IntegrityManager`. **Evidence:** `scripts/doorway/integrity.py`.
- [x] 3.2 Thread `readme_autoheal` passthrough in `DoorwayContextualizer`, explicit `False` at the real call site. **Evidence:** `scripts/doorway/doorway.py`.
- [x] 3.3 Update `scripts/tests/test_doorway.py`: existing 4 tests opt back in explicitly (`readme_autoheal=True`) to keep testing real self-heal regression behavior; 2 new tests (`TestReadmeAutohealDefaultOff`) prove the new default directly. **Evidence:** 6/6 passing.
- [x] 3.4 Remove breadcrumb README files from the working tree (`git rm` tracked, `rm` untracked) as part of this session's commit — safe now that 3.1-3.2 stop them regenerating. **Evidence:** see commit (Phase 9).
- [x] 3.5 Append a provenance note (not closure) to `helpdesk-tickets/20260705_sentinel-doorway-redesign_workflow.md` recording that its Phase 0 prerequisite is done and its Phase 1-6 architecture is folded into Phase 8 below. **Evidence:** note appended 2026-07-07, also documents that this ticket's own uncommitted governance artifacts (`MANIFEST.md`, `governance/Architecture.md`, root `README.md`) are this ticket's unresolved starting substrate, deliberately excluded from this session's commits rather than swept in.
- [ ] 3.6 **Explicitly not authorized, listed so it isn't silently lost:** git-history purge of the 8 historical commits touching `README.md` files (Tier B). Requires `git filter-repo` + force-push to the public remote. Do not execute without a fresh, separate, explicit user go-ahead — see governing ticket §5.3 and §7 risk entry. If that authorization is later given, this becomes its own task with its own acceptance criteria; none are specified here on purpose.

**Acceptance criteria:** 3.1-3.4 done and tested (yes, this session). 3.5 done. 3.6 stays unauthorized unless a future, separate instruction says otherwise.

---

## Phase 4 — Verification-Spine Re-Verification — **COMPLETE 2026-07-07** (all four re-verification targets — /execute-build, /secretary, /triage, /harden-workflow — have real, tested, read-only engines; all Claude judgment/build work, none delegated to Gemini)

- [x] 4.1 Re-run Honest-Design Discipline against current `/execute-build` (Sovereign v5, 2026-07-04/06) — confirm or correct the archived seed design before building. **Evidence:** full re-read of the live 519-line file plus the archived 2026-06-02 seed design; three-question test re-applied fresh. **Finding: seed design CORRECTED, not confirmed as-is.** Staged at `docs/compression-staging/execute-build-honest-design.md`. Key findings: (1) three of the seed design's four mechanical items are already built — `scripts/focus/phase_status.py` (built 2026-06-30/07-04 for `/focus-plan` + `/nodelete` Pillar 6) already parses `tasks.md` into a Phase Map with checkbox tallies and cross-references `BUILD_RECEIPTS.md` per phase; `/execute-build` just doesn't call it yet (Step 0b/0d/6 still say "read the file and extract by eye"). Building a second parser would be duplication, not new engine work. (2) The seed design's 4th mechanical item — frontmatter `phase_count` coherence — does NOT survive re-application of the Mock-Trap test: read either way (self-referential or against-target-tasks.md), it names a field this suite's documents don't actually produce; keeping it would mean inventing scope to check, not honest design. Dropped. (3) Two new, narrow mechanical checks were surfaced by reading current 5d/5f text directly (not in the 2026-06-02 one-line summary): a completeness-scan (grep for `TODO`/`FIXME`/`PLACEHOLDER`/etc., match-list only, no justification judgment) and a scope-diff (git-status set-difference against declared scope, no "was it warranted" judgment). (4) STRICT RULES 15-16 (added 2026-07-04, after the seed design was written) are confirmed irreducible judgment — Discussion Is Not Authorization and Turn-Boundary Pause Protocol both require reading intent/social context, not facts; no engine should touch either.
- [x] 4.2 Build `/execute-build`'s engine per the corrected design. **Evidence:** built `scripts/build/` — `__init__.py` (contract docstring), `evidence.py` (completeness scan + scope diff, pure functions, no judgment), `reporter.py` (human + JSON rendering), `build_audit.py` (CLI, imports `scripts/focus/phase_status.py`'s existing `build_phase_status_report()` directly rather than duplicating it). 15 new tests (`scripts/tests/test_build_evidence.py`) covering all 6 marker types, fenced-block skipping, missing-file degradation, git-unavailable degradation, rename handling, and an explicit read-only invariant test (filesystem snapshot + git HEAD unchanged before/after); full suite 318/318 passing (up from 295 pre-session). Live-run against this actual workspace confirmed sane output and correctly flagged a real out-of-scope file (`scripts/tests/test_build_evidence.py` itself, not yet in a declared scope list at time of test). Wired into `execute-build.md` Steps 0b (Phase Map via engine), 5d (Completeness Scan via engine), 5f (Scope Diff via engine) — each keeps an explicit manual-fallback instruction and keeps the justification/warranted-deviation judgment calls with the model, never the engine. `.md` hardened: GLOSSARY term added, frontmatter `dependencies` gains both scripts, version 5→6, `last_hardened` 2026-07-07, Change Log entry 11 appended. Lint: CLEAN (0 CRITICAL/WARNING) after `--fix-hashes --write`. Ticket opened and closed same session: `helpdesk-tickets/CLOSED_20260707_execute-build-engine-gap_workflow.md`. Symlink confirmed intact. **Incidental fix, found live during this build**: `scripts/build/` was silently git-ignored by an unanchored `build/` pattern in `scripts/gitignore/seed.toml` (matches any depth, not just repo root) — a real latent bug, same shape as the earlier `.history/archive/` gitignore-scoping incident. Anchored `dist/`/`build/` to `/dist/`/`/build/` in `seed.toml` and its `config.py` `DEFAULT_SEED` mirror, regenerated `.gitignore` via the seeder, confirmed `git check-ignore` now clears `scripts/build/`. Full suite re-confirmed 318/318 passing after the regeneration.
- [x] 4.3 Re-run Honest-Design Discipline against current `/secretary` (Sovereign v5, 571 lines, last hardened 2026-07-05). **Evidence:** full re-read; no seed design existed to confirm/correct (the 2026-06-02 queue only listed `/secretary` as PENDING #7 with no per-workflow sketch) — this is a first-time seed design, staged at `docs/compression-staging/secretary-honest-design.md`. **Finding**: unlike `/execute-build`, three of `/secretary`'s deterministic steps (suite linter, Suite Learning Registry, ledger growth) are already engine-backed and need no re-wiring. The real gap is different in kind: `/secretary` claims 4-5 artifacts were produced/updated every session (`SUITE_HEALTH.md`, manifest narrative shard, `HANDOFF.md`, `ANOMALY_LOG.md`, a `PROCESS_LEARNINGS.md` entry) with nothing structurally confirming any of them actually were — the file's own HOW TO BEGIN already contains a hand-written Hallucinated-Success warning ("Do not present SUITE_HEALTH.md... as the completion of /secretary"), which is exactly the kind of prose defense this campaign converts into a structural one. Four new mechanical checks identified, all formalizing steps ADDENDUM E / Step 0b.5 / the TRIAGE_RECEIPTS/DESIGN_RECEIPTS blocks already specify as shell commands, none inventing new scope: (1) artifact freshness (mtime check against session start / today), (2) retrospective presence+date-match (formalizes ADDENDUM E's `tail -n 10` + eyeball), (3) Retrospective Lag comparison (formalizes Step 0b.5's two greps into one `gap_detected` boolean), (4) receipt-family presence, generalized beyond the current two hardcoded blocks (TRIAGE_RECEIPTS/DESIGN_RECEIPTS) to any filename.
- [x] 4.3b Build `/secretary`'s engine. **Evidence:** built `scripts/secretary/` — `__init__.py` (contract docstring), `freshness.py` (mtime-vs-reference-time), `retrospective_check.py` (generalized last-dated-entry extraction covering both the `## YYYY-MM-DD` and `SESSION APPEND` conventions with one regex-parameterized function, plus the Retrospective Lag `gap_detected` boolean), `receipt_presence.py` (generalized existence+tail, parameterized by filename), `reporter.py`, `secretary_audit.py` CLI. 24 new tests (`scripts/tests/test_secretary_evidence.py`) including a read-only invariant test and a CLI-level regression test for a real defect caught during the live-run: `glob.glob()` does not expand `~` the way a shell does, so an unexpanded `--history-glob` silently found zero shards (a false "no gap" rather than an error) — fixed by `os.path.expanduser()` at the CLI boundary on every path-like argument (`--check-paths`, `--process-learnings`, `--receipts-dir`, `--history-glob`), not just the one flag where it was first noticed. Full suite 342/342 passing (up from 318 pre-task). Live-run against this actual workspace confirmed correct output post-fix (narrative and PROCESS_LEARNINGS.md correctly reported consistent). Wired into `secretary.md` Step 0b.5 (Retrospective Lag via engine), ADDENDUM E (retrospective date-match via engine), the TRIAGE_RECEIPTS/DESIGN_RECEIPTS blocks (folded into one generalized `receipt_family` call, old blocks kept as explicit fallback-only text), and new ADDENDUM F (Artifact Freshness Gate, Phase 7 — structurally backs the pre-existing HOW TO BEGIN Hallucinated-Success warning). Each wired step keeps an explicit manual-fallback instruction. `.md` hardened: GLOSSARY term added, frontmatter `dependencies` gains the engine, version 5→6, `last_hardened` 2026-07-07, Change Log entry 14 appended to `.changelogs/secretary.md`. Lint: CLEAN (0 CRITICAL/WARNING) after `--fix-hashes --write`. Ticket opened and closed same session: `helpdesk-tickets/CLOSED_20260707_secretary-engine-gap_workflow.md`. Symlink confirmed intact.
- [x] 4.4 Re-run Honest-Design Discipline against current `/triage` (Sovereign v3, 502 lines, last hardened 2026-05-25). **Evidence:** full re-read; staged at `docs/compression-staging/triage-honest-design.md`. **Finding**: `/triage` already reuses three existing engines directly in its Trigger Matrix (`harden_audit.py`, `iterate_audit.py`, `quality_audit.py`) plus `lint_workflows.py --quiet` — a real, working precedent for this exact discipline, confirming the pattern rather than requiring correction there. Two Phase 0 steps duplicate engine logic that exists elsewhere but isn't called from here: 0b (task/phase counting) hand-duplicates `scripts/focus/phase_status.py`; 0c (receipt presence) hand-duplicates `scripts/receipt/coverage.py`'s `compute_coverage()`. Both are pure wiring fixes, no new code. One genuinely new mechanical gap: **Trigger Matrix completeness** — STRICT RULES 3 and 9 plus Phase 1's own injected "Completeness requirement" independently state the same guarantee three separate times ("never omit a workflow from the report; omission is Hallucinated Success"), yet nothing mechanically confirms all ~24 Trigger Matrix blocks actually appear in the emitted report. Explicitly NOT built: wrappers around 0a's git commands, 0d's find/wc pipelines, or 0e's journal-staleness check — all three are already pure, zero-interpretation shell with no duplication anywhere in the suite; wrapping them would add ceremony, not rigor (this file's own Instruction Density Compression discipline warns against exactly that). 0g's Failure Pattern Surface Scan stays prose-guided — judgment-adjacent (flagging *potential* pattern evidence), same treatment as the Trigger Matrix's own P0-P3 assignments.
- [x] 4.4b Build `/triage`'s engine. **Evidence:** built `scripts/triage/` — `__init__.py` (contract docstring, explicit about the check's limits), `matrix_completeness.py` (`extract_matrix_workflows()` scoped to the `### Trigger Matrix` section only, `check_report_completeness()` set-difference), `reporter.py`, `triage_audit.py` CLI (combines matrix-completeness with direct imports of `phase_status.build_phase_status_report()` and `receipt.coverage.compute_coverage()` — no new parsing for either). 13 new tests (`scripts/tests/test_triage_evidence.py`) including a read-only invariant test; caught and fixed a real regex bug during test-writing (before the live-run) — annotated headers like `/quality`'s "(audit trigger)" sit inside the same bold span as the name, so the closing `**` doesn't immediately follow the name's backtick the way plain headers' does; fixed by dropping that trailing requirement. Full suite 355/355 passing (up from 342 pre-task). Live-run against this actual `triage.md` confirmed correct extraction: 25 distinct workflow entries, correctly distinguishing flagged variants (`/implementation-plan --workstreams` vs. bare `/implementation-plan`) and deduping `/focus-plan`'s two annotated blocks to one entry. Wired into `triage.md` Phase 0b (task/phase state via engine), Phase 0c (receipt coverage via engine), and a new Trigger Matrix Completeness Gate in Phase 2 — each keeps an explicit manual-fallback instruction, and the gate's own text explicitly states what it does NOT verify (name presence ≠ evaluation rigor). `.md` hardened: GLOSSARY term added, frontmatter `dependencies` gains the engine, version 3→4, `last_hardened` 2026-07-07, Change Log entry 13 appended. Lint: CLEAN (0 CRITICAL/WARNING) after `--fix-hashes --write`. Ticket opened and closed same session: `helpdesk-tickets/CLOSED_20260707_triage-engine-gap_workflow.md`. Symlink confirmed intact.
- [x] 4.5 Re-run Honest-Design Discipline against current `/harden-workflow` (Sovereign v4, 856 lines, last hardened 2026-07-04). **Evidence:** full re-read; staged at `docs/compression-staging/harden-workflow-honest-design.md`. **Finding**: this file already runs `scripts/suite/lint_workflows.py`, but only ONCE, late, at Phase 7d as a final blocking gate — every earlier phase asking the SAME structural questions (Phase 1 Assessment Card, Phase 4d Inter-Workflow Reference Integrity, Phase 5b `/triage` Compatibility Audit, Phase 7a/7c Completeness Check) re-derives them by eye instead of calling `scripts/suite/checks.py`'s already-exported, already-tested library functions (`parse_frontmatter`, `extract_glossary_terms`, `count_strict_rules`, `count_phases`, `check_symlinks`, `extract_workflow_refs`, `extract_workflow_refs`/`check_cross_references`). Four phases re-derive facts a fifth phase already confirms mechanically, near the end. Two genuinely new mechanical pieces: (1) the **Degradation Check** (Phase 1) — extracting `Standard Version: N` and comparing to the Current Standard Version is pure text-extraction-plus-arithmetic, not currently wrapped anywhere; (2) the **Grade Table** itself (THE SOVEREIGN STANDARD) states its four grades as an explicit binary decision table over presence/absence — computing a `grade_hint` from presence booleans is arithmetic over a fixed table, not a quality judgment, BUT the file's own STRICT RULE 4b text ("a STRICT RULES section that doesn't address the halt condition is incomplete") shows presence ≠ completeness — so the hint is scoped one-directional/advisory (mirrors the `/quality` v4 smell-linter precedent: "no smells found says NOTHING about quality"), never a certified grade. Also reuses `scripts/triage/matrix_completeness.py`'s `extract_matrix_workflows()` (built this same session, Phase 4.4b) for Phase 5b's `/triage` membership check — zero new code. Explicitly not built: `structured_output_present` has no existing reliable regex anywhere in the suite (receipt/report/certificate shapes vary too much) — reports `null` (unknown) rather than guessing wrong, matching the file's own honesty standard.
- [x] 4.5b Build `/harden-workflow`'s engine. **Evidence:** (a) small additive refactor to `scripts/suite/checks.py` — extracted `has_glossary_section`/`has_how_to_begin_section`/`has_strict_rules_section`/`has_integration_section`/`has_changelog_section` as standalone importable functions, `check_structure()` calling them internally; confirmed behavior-preserving via the existing 33-test `test_suite_checks.py` suite passing unchanged. (b) built `scripts/harden_workflow/` — `__init__.py` (contract docstring), `degradation_check.py` (`extract_standard_version()` — last mention wins — + `check_degradation()`, `CURRENT_STANDARD_VERSION = 3`), `grade_hint.py` (`compute_grade_hint()` — pure function implementing the Grade Table's literal decision logic, explicitly documents and resolves a real ambiguity in the source table's overlapping "Structured"/"Hardened" definitions, caps at "Hardened" whenever `structured_output_present` is unknown), `reporter.py`, `harden_workflow_audit.py` CLI (reuses `scripts/triage/matrix_completeness.py`'s `extract_matrix_workflows()` directly for the `/triage` gap check — zero new parsing). 23 new tests (`scripts/tests/test_harden_workflow_evidence.py`) including a CLI-level read-only invariant test and full coverage of the grade-hint decision ladder across all four grades. Full suite 378/378 passing (up from 355 pre-task). Live-run against two real workflows confirmed correct output: `harden-workflow.md` itself (grade_hint Hardened, capped by unknown structured_output, certified=current=3, represented in `/triage`) and `nodelete.md` (correctly identified as genuinely NOT represented in `/triage`'s Trigger Matrix — a real finding, not a bug, since `/nodelete` is a behavioral modifier invoked automatically). Wired into `harden-workflow.md` Phase 1 (structural facts + Degradation Check via engine), Phase 5b (`/triage` gap via engine), Phase 7c (Completeness Check via the same engine call, no re-derivation) — each keeps an explicit manual-fallback instruction, and the grade_hint text is explicit it is never a certified grade. `.md` hardened: GLOSSARY term added, frontmatter `dependencies` gains the engine, version 4→5, `last_hardened` 2026-07-07, Change Log entry 14 appended to `.changelogs/harden-workflow.md`. Lint: CLEAN (0 CRITICAL/WARNING) after `--fix-hashes --write`. Ticket opened and closed same session: `helpdesk-tickets/CLOSED_20260707_harden-workflow-engine-gap_workflow.md`. Symlink confirmed intact. **Closes Phase 4 entirely** — all four re-verification targets (`/execute-build`, `/secretary`, `/triage`, `/harden-workflow`) now have real, tested, read-only engines.

**Acceptance criteria:** each of the four gets a real, tested, read-only engine under `scripts/<name>/`; each `.md` hardened per the recipe's Step F; lint CLEAN; ticket opened and closed per Steps B/I. 4.1-4.2 (`/execute-build`) done first — Phase 6 depends on it.

---

## Phase 5 — Verification-Spine Engine Extraction (remaining 5) — **5.1-5.2b DONE (/redteam, /sentinel), 5.3-5.5 remain**

- [x] 5.1 Re-run Honest-Design Discipline against current `/redteam` (Sovereign v2, 468 lines, last hardened 2026-05-08). **Evidence:** full re-read; staged at `docs/compression-staging/redteam-honest-design.md`. **Structural finding, new to this campaign**: unlike the 4 Phase-4 targets, `/redteam` audits an arbitrary EXTERNAL codebase (Phase 0's own intake asks for "project root") — its target's test framework, mock library, log format, and DB schema are unknown until Phase 0 discovers them, unlike the other 4 engines which could assume this repo's own fixed conventions. This directly changes what's honestly buildable. **The seed design's own framing (tasks.md's prior "thin evidence rail (Ghost Logic collector)" note) does NOT survive the Mock-Trap test**: Phase 5's DB-event-vs-log reconstruction requires knowing a specific project's event/log schema — no schema-agnostic regex exists for "this log entry documents that decision." Building a generic version would mean inventing a fake schema and forcing every audited project to conform, which is scope invention (same disposition as dropping `/execute-build`'s `phase_count` coherence check). **Three narrower, genuinely schema-agnostic mechanical pieces replace it**: (1) mock enumeration (Phase 1b) — regex over Python source for `@patch`/`Mock()`/`MagicMock()`/`monkeypatch`, match-list only, VALID/TAUTOLOGY/UNREALISTIC classification stays judgment; (2) secret-pattern scan (Phase 3a) — the same keyword set already given verbatim, but with **structural redaction** (the matched value is always masked in output, closing a real gap: STRICT RULE 6 already prohibits exposing secret values, but nothing before this engine structurally prevented an agent from pasting a live grep match verbatim into a receipt); (3) coverage-gap parsing (Phase 1a) — reads `coverage.py`'s own stable `coverage json` schema (tool-owned, not project-specific) for below-threshold modules. Explicitly not built: Phases 2/3b/4 (fault injection, social engineering, Adversarial LLM Pressure — all require live execution against the target system, a fundamentally different problem than read-only evidence collection), Phase 1c (mutation testing — a write operation), Phase 1d (already a single minimal grep, wrapping it adds ceremony not rigor), Phase 6 (Remediation Report — the entire point of the workflow).
- [x] 5.1b Build `/redteam`'s engine. **Evidence:** built `scripts/redteam/` — `__init__.py` (contract docstring), `mock_scanner.py` (`scan_for_mocks()` — file/line/construct/snippet match list, all 4 construct types), `secret_scanner.py` (`scan_for_secrets()` — the matched value structurally EXCLUDED from the result object's shape entirely, not merely displayed redacted), `coverage_gap.py` (`parse_coverage_json()` — reads `coverage.py`'s own JSON schema, 80%/100% two-tier threshold), `reporter.py`, `redteam_audit.py` CLI. **Live, real finding during the honest-design pass, not just theoretical**: the PRE-FIX Phase 3a text instructed a plain `grep -n`, which would have printed the actual secret value directly into the agent's own output — a live self-contradiction of the same file's STRICT RULE 6. 19 new tests (`scripts/tests/test_redteam_evidence.py`) including a read-only invariant test, a redaction test at the Python-object level, and a second redaction test at the CLI/JSON-stdout boundary (confirms a live test secret value is genuinely absent from the actual text an agent would read, not just the internal object). Full suite 397/397 passing (up from 378 pre-task). Live-run confirmed correct behavior scanning this suite's own test/source files. Wired into `redteam.md` Phase 1a (coverage-gap parsing via engine), Phase 1b (mock enumeration via engine, tautology assessment stays manual), Phase 3a (secret scan via engine — explicit warning added against the old plain-grep approach, redaction-safe fallback given). `.md` hardened: GLOSSARY term added, frontmatter `dependencies` gains the engine, version 2→3, `last_hardened` 2026-07-07, Change Log entry 3 appended. Lint: CLEAN (0 CRITICAL/WARNING) after `--fix-hashes --write`. Ticket opened and closed same session: `helpdesk-tickets/CLOSED_20260707_redteam-engine-gap_workflow.md`. Symlink confirmed intact.
- [x] 5.2 Re-run Honest-Design Discipline against current `/sentinel` (Sovereign v3, 544 lines, last hardened 2026-07-07). **Evidence:** full re-read; staged at `docs/compression-staging/sentinel-honest-design.md`. **Finding: the seed design's assumed gap doesn't exist.** `/sentinel` already delegates almost entirely to real engines (`doorway.py`, `gitignore_seeder.py`, `ensure_plan_templates.py`); the tasks.md seed note's "drift-delta layer" is already built — `doorway.py`'s own snapshot/hash-compare (`_load_snapshot`/`_save_snapshot`) already computes `drift.new/modified/deleted` session-over-session. **The real gap, found by direct comparison rather than assumption**: Phase 2b's Routing Map table hand-duplicates logic `scripts/doorway/recommender.py` already owns and emits per-recommendation via its own `workflow` field — and diffing the two directly (not just reading the `.md`'s claim about itself) found the duplicate had ALREADY DRIFTED, live, in two ways: (1) `recommender.py` emits a second `SEQ-SUBSTRATE-HEALTH` recommendation for Tier-1 issues (`stale_index`/`ownership_incomplete`) with no corresponding row in Phase 2b's table at all; (2) `recommender.py` emits `severity: "INFO"` for `SEQ-SUBSTRATE-MAINTAIN`, but Phase 2a's Severity Tally only counts HIGH/MEDIUM/LOW and the GLOSSARY's Severity Tier term only names those three — an INFO-severity recommendation is silently uncounted (included in `TOTAL` but in none of the three tallied buckets). This is a live instance of the exact failure this campaign exists to close, caught by comparing prose against the engine it describes rather than trusting the prose.
- [x] 5.2b Build `/sentinel`'s engine. **Evidence:** built `scripts/sentinel/` — `__init__.py` (contract docstring), `recommender_parity.py` (`extract_recommender_triples()` — regex over `recommender.py`'s `recs.append({...})` blocks; `extract_routing_table()` — parses Step 2b's markdown table; `compute_parity()` — set-difference PLUS a `Counter`-based occurrence-vs-row-count check, the specific mechanization needed since presence alone misses the duplicate-ID defect), `reporter.py`, `sentinel_audit.py` CLI. Fixed both discovered live defects directly in `sentinel.md`: added the missing Tier-1-issues `SEQ-SUBSTRATE-HEALTH` row to Step 2b's table; added `INFO` to the Severity Tier GLOSSARY term and Step 2a's tally with an explicit `HIGH+MEDIUM+LOW+INFO == TOTAL` invariant check. 14 new tests (`scripts/tests/test_sentinel_evidence.py`) including a read-only invariant test and REGRESSION tests proving the checker catches both real defects found in this repo's own pre-fix file (not just clean-input tests). Full suite 411/411 passing (up from 397 pre-task). Live-run against the real `recommender.py`/`sentinel.md` pair confirmed both defects pre-fix and `PARITY: CLEAN` post-fix. Wired Step 2b with a live verification command and a note that this exact table was found drifted during this pass. `.md` hardened: GLOSSARY term added, frontmatter `dependencies` gains the engine, version 3→4, `last_hardened` 2026-07-07, Change Log entry 7 appended. Lint: CLEAN (0 CRITICAL/WARNING) after `--fix-hashes --write`. Ticket opened and closed same session: `helpdesk-tickets/CLOSED_20260707_sentinel-engine-gap_workflow.md`. Symlink confirmed intact.
- [ ] 5.3 `/continuous-verify` — plan-alignment cross-checker, reusing `scripts/focus/anchor_scanner.py`.
- [ ] 5.4 `/investigate` — read-only forensic anchoring / citation fidelity report.
- [ ] 5.5 `/helpdesk-tickets` — ticket lifecycle engine (OPEN/CLOSED counts, schema validation, staleness detection).

**Acceptance criteria:** same as Phase 4, per workflow.

---

## Phase 6 — Handoff Mechanism
**SUPERSEDED [QUARANTINE:2026-07-07]** — the 5 tasks below assumed a new `/workstream` mode was needed. It isn't. See `implementation-plan.md`'s corrected Phase 6. Struck through, not deleted, per `/nodelete`.

- [x] ~~6.1 Extend `/workstream` Phase 0a to recognize a workstream design with only Workstream B populated (A/C explicitly DORMANT in `implementation-plan.md`'s Roles table, not silently absent).~~ **Not needed — `/execute-build` already works directly.**
- [x] ~~6.2 Extend `/implementation-plan` Phase 6 (Workstream Design) to support generating a single-engineer design...~~ **Not needed.**
- [x] ~~6.3 Confirm Pre-Flight Manifest, Engineer Brief, Handoff Block work unmodified in single-engineer mode...~~ **Not needed — `/execute-build` has its own equivalent machinery (Phase 0 discovery, Phase Build Receipts).**
- [x] 6.4 Bounded-scope safety principle — **carried forward, not superseded**: every Gemini delegation is scoped to one `tasks.md` phase at a time (already true by construction — `/execute-build` builds one phase, issues one receipt, and advances only on success). No new STRICT RULE needed; `/execute-build`'s own Phase 6 loop already enforces this shape.
- [x] ~~6.5 Confirm /implementation-plan --audit --workstreams runs cleanly against a single-populated workstream...~~ **Not needed — not using `/workstream` for this.**

**Resolution:** open an Antigravity session, invoke `/execute-build`, point it at this workspace. Done — verified 2026-07-07 by re-reading `execute-build.md` in full (no Claude-Code-specific assumptions found; format matches).

---

## Phase 7 — User Training Guide

- [ ] 7.1 Write `docs/GEMINI_EXECUTE_BUILD_GUIDE.md`: how to open an Antigravity session and invoke `/execute-build`, what the Phase Map / Phase Build Receipt output looks like, how to tell if a phase HALTed on underspecification vs. completed, and how to carry results back (a `git log`/`git diff` review is enough — no special handoff file format needed since `/execute-build` writes directly to the shared workspace).
- [ ] 7.2 After Phase 8's pilot runs, add the worked example from that real run to the guide (not a hypothetical one).

**Acceptance criteria:** the guide exists, is accurate against `/execute-build`'s actual behavior (verified against the real file, not assumed), and was genuinely used for Phase 8 rather than written after the fact from memory of how it went.

---

## Phase 8 — First Live Delegation Pilot

- [x] 8.1 Confirm Phase 1 is still `[ ]`/unstarted at pilot time. **Evidence:** confirmed prior to user invoking Gemini — 1.2/1.3 were `[ ]` at time of handoff.
- [x] 8.2 Open an Antigravity session in this workspace; invoke `/execute-build`. **Evidence:** user-confirmed, this session.
- [x] 8.3 Let it run Phase 1 (1.2, 1.3 — 1.1 is already done) to completion or to a legitimate HALT. **Evidence:** `BUILD_RECEIPTS.md` — "2026-07-07 — /execute-build — Phase 1 — Quick Wins — PHASE COMPLETE"; Gemini halted at the phase boundary as designed (bounded-scope principle, Phase 6.4) and touched only `tasks.md`'s own 1.2/1.3 checkboxes as its final action.
- [x] 8.4 Claude reviews the result. **Evidence:** independent audit performed 2026-07-07 (not a re-read of Gemini's "Evidence:" claims) — byte-diff confirmed identical Change Log extraction for all 5 `.changelogs/*.md` files against the pre-Gemini `git show HEAD:...` originals; pointer text verified against the exact template (entry counts and dates independently recounted via `grep -c`, not trusted from the pointer text itself); `content_hash` recomputed correctly for all 5 (bit-exact match against a fresh `lint_workflows.py --fix-hashes` computation); lint scan of the 5 target files confirmed 0 CRITICAL/WARNING (suite-wide WARNINGs present are all in unrelated pre-existing files — `design-orchestrator.md`, `nodeleteshort.md`, `refactor.md`); file-mtime evidence (13:20:53–13:21:44 window) confirmed zero scope creep — only the 5 workflow files, `.changelogs/`, the HTML report, and `tasks.md` were touched, not the pre-existing uncommitted scaffolding already sitting in the working tree. One minor finding: `BUILD_RECEIPTS.md`'s `Commit:` field cites `9491958`, a stale pre-Gemini hash (nothing was actually committed — correctly deferred to Phase 9.5) — cosmetic only, logged in `PROCESS_LEARNINGS.md`, does not affect acceptance. **Verdict: PASS.**
- [x] 8.5 Record the outcome in `PROCESS_LEARNINGS.md`. **Evidence:** entry appended 2026-07-07, "Sovereign Scaling Cluster: First Live Gemini Delegation (Phase 1)" — PASS, one LOW-priority workflow-improvement suggestion logged (receipt `Commit:` field should detect the batched-commit case rather than citing a stale HEAD).
- [ ] 8.6 Only after 8.1-8.5 succeed: begin the design-tightening pass on Phase 2, 4, or 5 (whichever the user prioritizes) so the NEXT delegation has a real precedent to build on, not just a hypothesis.

**Acceptance criteria:** Phase 1 is genuinely built by Gemini and genuinely verified by Claude, with an honest retrospective regardless of outcome, before any other phase is attempted. **Met 2026-07-07.**

---

## Phase 9 — Session Close (this session's own authorized cleanup)

- [x] 9.1 Close `helpdesk-tickets/20260704_lint-fix-hashes-gap_workflow.md` with a real Remediation Record (Phase 1.1 resolves it).
- [x] 9.2 Close `helpdesk-tickets/20260705_triage-session-handover_workflow.md` — its one live recommendation independently confirmed already built (`TRIAGE_RECEIPTS.md` exists, wired in `triage.md:426`).
- [ ] 9.3 Run full test suite (`./run_tests.sh`) — must show all tests passing.
- [ ] 9.4 Lint every `.md` touched this session — 0 CRITICAL.
- [ ] 9.5 Commit this session's real work (code changes, ticket updates, plan/tasks files, README removal). Do not push without a separate explicit request.

**Acceptance criteria:** 9.1-9.2 done with real Remediation Records (not just renamed). 9.3-9.5 complete before this session ends.

---
