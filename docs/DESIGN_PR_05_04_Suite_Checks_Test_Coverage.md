---
description: "Small native DESIGN for PR 05-04 (linter robustness) — Stage 4 end-to-end vehicle proving /design-orchestrator's real native path + /execute-build's Native Execution Trigger, Sovereign Redesign Cluster"
---

# DESIGN: Direct Unit Test Coverage for scripts/suite/checks.py's Core Functions

**Author:** Claude Code, via `/design-orchestrator`'s native path (Phase 0-5, no Grok involved) — Sovereign Redesign Cluster Stage 4, Task 4.4 end-to-end vehicle
**Date:** 2026-07-06
**Status:** Draft, independently critiqued (see Independent Critique below)
**Governing:** `docs/design-pillars/PILLAR_05_TOOLING_LINTING_RUNTIME_GOVERNANCE.md` PR 05-04 ("linter robustness + tests + baseline clean")

---

## [INTENT]

> `scripts/tests/test_suite_checks.py` — the only test file for `scripts/suite/checks.py`, the module that implements the entire Sovereign Suite linter's parsing and validation logic — exercises exactly 2 of its 14 functions (`check_runtime_availability`, `check_symlinks`), both added specifically for the OpenCode→Grok-Build transition ticket. The other 12 functions (`parse_frontmatter`, `compute_content_hash`, `extract_workflow_refs`, `_extract_strict_rules_section`, `count_strict_rules`, `count_phases`, `extract_glossary_terms`, `check_frontmatter`, `check_structure`, `check_cross_references`, `check_content_hash`, `check_glossary_usage`) have zero direct unit tests — the test file's own docstring admits this: "this suite predates unit tests entirely and a full retrofit is out of scope for this fix." Add direct, real unit coverage for the functions most load-bearing to linter correctness — a bug in `compute_content_hash` or `parse_frontmatter` would silently corrupt every hash-verification and structural check in the entire suite, and nothing today would catch it.
>
> Marked /nodelete: not applicable (net-new small DESIGN).

---

## Investigation (evidence-based — real focus-plan Evidence Report, not assumption)

- **Primary payload (focus-plan Evidence Report, run live this session):** `verdict_hint: YELLOW`, 10 items evaluated, `tasks_md.found: False` (root-relative lookup, expected — this DESIGN's own tasks.md doesn't exist yet). No mock-trap candidates.
- **Sentinel briefing (doorway --context-only, run live this session):** `zero_finding: True`, 33 directories tracked in the substrate index — workspace state is clean and current, no stale context risk for this DESIGN.
- **Negative Space Scan:** Beyond the untested functions themselves — is there a reason NONE of them were tested historically? Checked: `test_suite_checks.py`'s docstring frames itself as scoped narrowly ("Covers: only the behavior changed 2026-07-05... Pre-existing linter behavior... is not otherwise covered here") — meaning this is a known, named, deliberately-deferred gap, not an oversight nobody noticed. This DESIGN closes exactly the deferred scope, nothing more.
- `scripts/suite/checks.py:26-90` (`parse_frontmatter`, `compute_content_hash`, `extract_workflow_refs`, `_extract_strict_rules_section`, `count_strict_rules`, `count_phases`, `extract_glossary_terms`): pure parsing functions, no side effects, straightforward to unit test with crafted markdown strings.
- `scripts/suite/checks.py:90-171` (`check_frontmatter`, `check_structure`, `check_cross_references`): report-populating functions taking a `LintReport` object — same test shape as the existing `check_runtime_availability`/`check_symlinks` tests (construct a `LintReport()`, call the function, assert on `.findings`).
- `scripts/suite/checks.py:225-244` (`check_content_hash`, `check_glossary_usage`): same shape.

## Scope & Boundaries

**In scope:** direct unit tests for the 12 untested functions listed above, in `scripts/tests/test_suite_checks.py` (extending the existing file, not creating a new one — it already owns this module's test surface).
**Out of scope:** any change to `checks.py`'s actual logic (this is a test-coverage DESIGN, not a bug-fix DESIGN — if testing surfaces a real bug, that becomes its own follow-up, not silently fixed inline here per Mock Trap avoidance: don't let "add tests" quietly become "also change behavior" without that being the stated intent).

## Build Ingestion Manifest

- **Intent Anchor:** this DESIGN's own `[INTENT]` section above; /nodelete not applicable (net-new).
- **Gaps & Divergences:** none found beyond the stated scope — Negative Space Scan confirms this is a fully-named, bounded gap.
- **Verification:** focus-plan Evidence Report (live, this session, YELLOW/10 items/no mock-trap-candidates); post-gate re-verify below.
- **Native Gates Mapping:** this DESIGN's own tasks.md (once generated by `/implementation-plan` via `/execute-build`'s Native Execution Trigger) requires `/focus-plan` PARITY or PENDING before its Phase Build Receipt; continuous-verify per `execute-build.md` 5g; receipt format `BUILD_RECEIPTS.md` (this is a test-only change with no separate DESIGN_RECEIPTS-worthy design decision beyond what's captured here — though this DESIGN itself still gets its own DESIGN_RECEIPTS entry per Phase 4 below); PR Plan below is the direct input to `/implementation-plan`.
- **Substrate Hygiene:** zero_finding True (Pillar 1, live this session); no `/divergence --convergence` run (scope too narrow to warrant one — pure test addition, no dead substrate risk).

## Acceptance Criteria (measurable)

1. Each of the 12 currently-untested functions in `checks.py` has at least one direct unit test exercising its real logic (not a mock of it).
2. Tests follow the existing file's established pattern (construct real `LintReport()`/frontmatter dicts/markdown strings, assert on real return values or `.findings`).
3. No change to `checks.py`'s actual logic — if a test reveals a real bug, it is named as a finding, not silently patched.
4. Full test suite still passes; new test count is verifiable via `grep -c "^    def test_"`, not asserted from memory.

## PR Plan

**PR 05-04 (single, small): Direct unit coverage for checks.py's untested core functions**
- Files: `scripts/tests/test_suite_checks.py` (extend with ~12 new test methods, one per currently-untested function).
- Dependencies: none (pure test addition, no other PR needed first).
- Description: implements this DESIGN's Acceptance Criteria 1-4 directly — one test class or set of test methods per function group (pure parsing functions; report-populating functions), following the file's own existing `LintReport()`-construction pattern.

---

## Independent Critique (native path, no Grok — `/quality` Step 5 adversarial self-critique, per §15 Mock Trap guard)

**Critique 1:** Is "at least one test per function" a low enough bar to be gameable (a trivial smoke test that doesn't actually verify behavior)? — Addressed: Acceptance Criterion 2 requires exercising "real logic," and the actual test implementation (next task) must assert on genuinely meaningful outputs (e.g., `parse_frontmatter` tests must assert the parsed dict's actual field values, not just "it returned something"), not merely call the function and check it didn't crash.

**Critique 2:** Should this DESIGN also test the CLI/orchestration layer (`lint_single`, `main()` in `lint_workflows.py`) since those are equally "linter robustness"? — Considered and scoped out deliberately: `test_integration.py`-style subprocess tests already exercise the CLI indirectly (e.g., via `--output-json` calls throughout this session's own work), and the specific, named gap (per the docstring's own admission) is `checks.py`'s functions specifically, not the orchestration layer. Expanding scope here would dilute a well-bounded PR into a vaguer one — rejected in favor of staying precisely targeted.

**Zero remaining open issues.** Ready for handoff via `/execute-build`'s Native Execution Trigger.
